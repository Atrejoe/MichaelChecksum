apiVersion: v1
kind: Service
metadata:
  name: michaelchecksum
  namespace: michaelchecksum
  labels:
    app: michaelchecksum
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: michaelchecksum
